language: node_js
sudo: false
cache:
  directories:
    - "node_modules"
before_install:
  - nvm install stable
  - npm i -g standard
  - npm i -g mocha
install:
  - travis_retry npm install
script:
  - npm test